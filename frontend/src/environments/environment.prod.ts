export const environment = {
  production: true,
  // After backend is live on Render, replace with your real API URL:
  // Example: 'https://shopease-api.onrender.com/api'
  apiUrl: 'https://YOUR-BACKEND-NAME.onrender.com/api'
};
